# Bucket: build

Progres implementasi fitur SPK Koperasi. Newest-first.

- 2026-06-10 04:20 WIB — PangeranSilaen (via OpenCode): POLISH UI lanjutan, branch `fix/ui-polish-dropdown-toast-tabel`, PR #19, issue #18. (a) Base UI `Select` menampilkan cuid kalau tidak diberi prop `items` (peta value→label) di `Select.Root` — WAJIB pasang `items` agar `SelectValue` meresolusi label. (b) Toaster: hapus next-themes, paksa `theme="light"` (app light-only). (c) Dropdown nilai strategi w-16→w-14. (d) Warning Base UI "uncontrolled FieldControl" muncul saat Input pakai `defaultValue` lalu data di-refresh → ubah SEMUA input dialog (expert/kriteria/coded-entity) jadi controlled + reset di handleOpenChange saat dibuka. (e) /ahp: opsi perbandingan jadi grid 3 kolom + baris intensitas terpisah (hilangkan gap tengah). (f) Tabel /riwayat: hapus `p-0` di CardContent (pakai padding default seperti /expert). Verifikasi: lint 0/0, 33 test, build sukses. Issue #11/12/13/15/16 di-CLOSE (selesai PR #17); #9/10/14 dikomentari (sisa di #18). Workflow: PR #17 sudah dimerge user, pull main dulu sebelum branch baru.

- 2026-06-10 02:45 WIB — PangeranSilaen (via OpenCode): OVERHAUL UI + fitur, branch `redesign/ui-overhaul-dan-fitur`, PR #17. Tinggalkan tema Binance → dashboard pro modern (slate netral + aksen indigo `#4f46e5`, light-default). PRODUCT.md + DESIGN.md baru. Ganti SEMUA `<select>` native dengan komponen Select Base UI (catatan: `onValueChange` mengirim `string|null` → bungkus `(v)=>setX(v ?? "")` agar lolos type check). Hapus glyph "SPK" + link liar "Kembali". Grid AHP `[1fr_auto_1fr]` agar "vs" di tengah. ConfirmDialog reusable (anti double-submit) untuk duplikat/publish/arsip + restoreModelAction (Arsip→Draf) + deleteModelAction (Aktif tak bisa dihapus). Riwayat kalkulasi + nama nasabah (PERUBAHAN PRIVASI: customerName disimpan di CalculationRun). AuditLog model + halaman /audit & /riwayat. suppressHydrationWarning untuk ekstensi browser. Pasang skill impeccable di .agents/skills (butuh PRODUCT.md, ESLint ignore .agents/.multibrain). Migrasi `20260609192500_add_audit_log_and_customer_name`. Verifikasi: lint 0/0, 33 test lulus, build sukses. GitHub issues feedback #8-16 dibuat. Git identity diperbaiki ke PangeranSilaen + noreply.
- 2026-06-09 17:55 WIB — OpenCode: SELESAI seluruh 9 fase MVP + export PDF. Verifikasi: 33 unit test lulus, `npm run build` sukses, `npm run lint` 0 error, render PDF valid (%PDF-), parser XLSX end-to-end (1 expert/6 kriteria/7 alt/5 kondisi/15 pairwise/210 nilai, 0 error). Stack final: Next.js 16 + React 19 + Prisma 6 (downgrade dari 7 karena breaking changes) + shadcn Base UI preset (pakai `render` prop bukan `asChild`) + Tailwind 4. Tema Binance dark di globals.css (DIGANTI 2026-06-10). -> .multibrain/context/2026-06-09-1755-opencode-mvp-selesai.md
- 2026-06-09 23:13 WIB — OpenCode: mulai penulisan implementation plan di `docs/superpowers/plans/`. Belum ada kode aplikasi.
